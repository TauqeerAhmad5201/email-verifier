apiVersion: v1
kind: Service
metadata:
  name: email-verifier-blue
  namespace: email-verifier
spec:
  selector:
    app: email-verifier
    version: blue
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP 
